<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
        }

        h1 {
            margin-bottom: 20px;
        }

        form {
            margin-bottom: 20px;
        }

        input[type="text"],
        input[type="submit"] {
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
            width: 200px;
            margin-right: 10px;
        }

        input[type="submit"] {
            background-color: #4CAF50;
            color: white;
            cursor: pointer;
        }

        a {
            text-decoration: none;
            color: #0066cc;
        }

        table {
            border-collapse: collapse;
            width: 100%;
        }

        th,
        td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #f2f2f2;
        }

        form.inline-form {
            display: inline-block;
        }

        form.inline-form input[type="submit"] {
            margin-top: 0;
        }
    </style>
</head>

<body>
    <h1>Members</h1>
    <div>
        <form method="get" action="listMember" class="inline-form">
            <input type="text" name="name" placeholder="Name" value="<%= query.Name %>" />
            <input type="text" name="phoneNumber" placeholder="Phone Number" value="<%= query.Phone %>" />
            <input type="submit" value="Search" />
        </form>

        <div>
            <p><a href="addMembers">Add Member</a></p>
        </div>
        <div>
            <p><a href="overdueMembers">Show Overdue members</a></p>
        </div>
        <div>
            <p><a href="listbook">Show All Books</a></p>
        </div>
    </div>
    <table>
        <thead>
            <tr>
                <th>MemberId</th>
                <th>Name</th>
                <th>Address</th>
                <th>Phone</th>
                <th>Payment</th>
                <th>Role</th>
                <th>Age</th>
                <th>Remove Member</th>
                <th>Update Member</th>
            </tr>
        </thead>
        <tbody>
            <% for (var i=0; i < members.length; i++) { %>
                <tr>
                    <td>
                        <%= members[i].MemberID %>
                    </td>
                    <td>
                        <%= members[i].Name %>
                    </td>
                    <td>
                        <%= members[i].Address %>
                    </td>
                    <td>
                        <%= members[i].Phone %>
                    </td>
                    <td>
                        <%= members[i].Payment %>
                    </td>
                    <td>
                        <%= members[i].Role %>
                    </td>

                    <td>
                        <%= members[i].Age %>
                    </td>
                    <td>
                        <form method="POST" action="/deleteMember" class="inline-form">
                            <input type="hidden" name="id" value="<%= members[i]._id %>">
                            <input type="submit" value="Delete">
                        </form>
                    </td>
                    <td><button style="background-color: #4CAF50; color: white;"><a
                                href="updateMember/<%= members[i]._id %>">Edit</a></button></td>
                </tr>
                <% } %>
        </tbody>
    </table>
</body>

</html>